spec:
  name: timecobot
  databases:
    - cluster_name: redis
      engine: REDIS
      name: redis
      production: true
      version: "6"
  workers:
    - name: backend
      dockerfile_path: Dockerfile
      github:
        repo: demidko/timecobot
        branch: main
        deploy_on_push: true
      envs:
        - key: TOKEN
          type: SECRET
          scope: RUN_TIME
        - key: DATABASE_URL
          scope: RUN_TIME
          value: ${redis.DATABASE_URL}